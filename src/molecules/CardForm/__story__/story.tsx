import * as React from 'react';
import {storiesOf} from '@storybook/react';
import {CardForm} from '..';

storiesOf('Molecules|CardForm', module).add('Defaults', () => <CardForm>Hello world!</CardForm>);
